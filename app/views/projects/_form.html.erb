<%= form_with(model: project, html: { class: 'project__form form-inline', autocomplete: 'off' }) do |f| %>
  <%= f.text_field :name,
    placeholder: 'Имя проекта',
    class: 'form-control project__name-field rounded-0 px-0' %>
  <%= f.submit class: 'btn btn-success rounded-0 shadow-sm ml-3' %>
<% end %>
