$('.task__form').off('ajax:success').bind('ajax:success', function() {
  const task = $(this).parents('.task');

  task.find('.task__title').html("<%=  @task.name %>")
  task.find('.task__expire-date').html("<%= @task&.expires %>")
});
