$('.task__edit-button').off('ajax:success').bind('ajax:success', function() {
  const task = $(this).parents('.task');

  task.find('.task__title').html("<%= j render "tasks/form", project: @task.project, task: @task %>")
});
